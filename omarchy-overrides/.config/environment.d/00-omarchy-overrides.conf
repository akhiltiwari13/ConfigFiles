# Prepend ~/.config/bin to PATH so stow-managed shims (e.g.
# omarchy-refresh-tmux block) shadow ~/.local/share/omarchy/bin entries.
# Read by systemd user manager at session start; supports $HOME interpolation.
PATH=$HOME/.config/bin:$PATH
